events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # 1. THE CHALLENGE SERVER (Port 80)
    server {
        listen 80;
        server_name khadija-gradioapp.larnova.ng;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # 2. THE SSL SERVER (Port 443)
    # UNCOMMENT THIS SECTION ONLY AFTER CERTBOT SUCCEEDS
    # server {
    #     listen 443 ssl;
    #     server_name khadija-gradioapp.larnova.ng;
    #
    #     ssl_certificate /etc/letsencrypt/live/khadija-gradioapp.larnova.ng/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/khadija-gradioapp.larnova.ng/privkey.pem;
    #
    #     location / {
    #         proxy_pass http://gradio:7860;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #
    #         # Needed for Gradio WebSockets
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #     }
    # }
}